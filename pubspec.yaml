name: video_viewer
description: Multiplatform minimalist video viewer with spectacular user experience.
homepage: https://github.com/seel-channel/video_viewer
version: 1.2.7

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  gesture_x_detector: ^1.0.0
  helpers: ^1.2.3
  http: ^1.1.2
  path_provider: ^2.0.6
  provider: ^6.0.1
  volume_watcher: ^2.0.1
  wakelock_plus: ^1.1.4
  win32: ^5.1.1
  logger: ^2.0.2+1
  video_player:
     git:
       url: https://github.com/ologunB/packages.git
       ref: main
       path: packages/video_player/video_player


dev_dependencies:
  flutter_test:
    sdk: flutter
